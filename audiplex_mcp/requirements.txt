mcp>=1.2.0
httpx>=0.27.0
# Ingest (dj_find_candidates / dj_ingest). yt-dlp also needs ffmpeg on PATH to
# produce .m4a; mutagen writes the tags. Both are imported lazily, so the other
# 21 tools still work if these are missing.
yt-dlp>=2025.1.1
mutagen>=1.47.0
